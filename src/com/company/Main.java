package com.company;

public class Main {
    public static void createConwayGame(int size) {
        // It's basically a grid, there may be an initial configuration like the grid size

        Boolean [][] conwayMaps = new Boolean[size][size];
    }

    public static void main(String[] args) {
        createConwayGame(4);
    }
}
